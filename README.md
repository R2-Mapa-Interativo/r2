## Rodando localmente

### Com Docker

```bash
docker compose up --build
```

Depois acesse:

```text
http://localhost:8080
```

Para parar:

```bash
docker compose down
```

### Sem Docker

```bash
npm ci
npm run dev
```

Depois acesse:

```text
http://localhost:8080
```
